step 1: extract both "CRUD-APP" and "json-server-master"
step 1: In both folder run command "npm install"
step 2: Then run command "npm start" in both folders
step 3: Then go to http://localhost:4000 and wait for a minute